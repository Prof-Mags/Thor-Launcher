<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      A deliberately minimal platform theme. All real styling is done in Compose
      by ThorTheme; this exists only to give the window a dark background before
      the first frame and to make the system bars transparent so the launcher
      can draw edge to edge.
    -->
    <style name="Theme.Thor" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">false</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
    </style>
</resources>
